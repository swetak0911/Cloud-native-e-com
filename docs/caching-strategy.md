# Caching Strategy

> Starter notes — expand as you build.

Redis for product detail (cache-aside), cart (write-through), session tokens. TTLs, invalidation on price/stock change, stampede protection.
